  # authorizer:
  #   handler: authorizer.authorizer
  handler:
    handler: signUp.handler  # Corrected filename and path
    events:
      - http:
          path: /signUp
          method: post
  signin:
    handler: signIn.signIn
    events:
      - http:
          path: /signIn
          method: post
